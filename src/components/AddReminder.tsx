import { Button } from "@/components/ui/button";

export function AddReminder() {
  return (
    <div className="text-center mt-6">
      <Button className="bg-green-600 text-white">➕ Add Reminder</Button>
    </div>
  );
}
